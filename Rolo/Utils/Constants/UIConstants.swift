import Foundation

enum UIConstants {
    
    enum ImageNames {
        static let refresh = "Refresh"
        static let handleUpload = "HandleUpload"
        static let googleLogo = "Google logo"
        static let stripe = "Stripe"
        static let help = "Help"
        static let arrowDown = "ArrowDown"
        
        enum TabBar {
            static let dashboard = "Dashboard"
            static let dashboardSelected = "DashboardSelected"
            static let members = "Members"
            static let membersSelected = "MembersSelected"
            static let donations = "Donations"
            static let donationsSelected = "DonationsSelected"
            static let profile = "Profile"
        }
    }
    
    enum IconSizes {
        static let small = 16.0
        static let medium = 20.0
        static let large = 24.0
        static let extraLarge = 32.0
    }
    
    enum LogoSize {
        static let width = 60.0
        static let height = 60.0
    }
    
    enum ButtonHeight {
        static let standard = 58.0
    }
    
    enum TextFieldHeight {
        static let empty = 48.0
        static let filled = 28.0
    }
}
